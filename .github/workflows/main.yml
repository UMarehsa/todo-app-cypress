name: ci
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2.6.0
      - name: Cypress.io
        uses: cypress-io/github-action@v5.0.0 
        with:
          start: npm start
      
          
  
  
  
